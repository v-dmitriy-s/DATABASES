/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'play.google.com',
            'developer.apple.com',
            'picsum.photos'
        ],
    },
}

module.exports = nextConfig 